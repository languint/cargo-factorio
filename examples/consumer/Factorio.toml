source = "src"
output_dir = "dist"

[mod]
title = "consumer"
description = "Example mod that depends on provider via ephemeral binding stubs"
factorio_version = "2.0"
# Binding crates also contribute deps; this shows an explicit extra entry.
dependencies = ["? space-age", "provider >= 0.1.3"]
